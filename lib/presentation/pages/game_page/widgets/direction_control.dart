import 'package:flutter/material.dart';

class DirectionControl extends StatelessWidget {
  DirectionControl({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 100,
      width: 100,
      child: Container(
        color: Colors.blueGrey,
        child: Column(
          children: [
            
          ],
        ),
      ),
    );
  }
}
